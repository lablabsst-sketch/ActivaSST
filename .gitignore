# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env / secrets
# Nota: .env actual está commiteado a propósito (solo contiene la anon key pública
# que Lovable gestiona). Cualquier variante con claves sensibles (service_role,
# tokens privados, VAPID private) NUNCA debe commitearse.
.env.secret
.env.production
.env.local
!.env.example

# Claude local artifacts that may carry credentials (skills dir SÍ se commitea)
.claude/*.local.*

# Supabase local
supabase/.branches
supabase/.temp

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
